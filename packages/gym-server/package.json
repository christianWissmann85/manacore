{
  "name": "@manacore/gym-server",
  "version": "0.1.0",
  "description": "HTTP server exposing ManaCore engine API for Python ML frameworks",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@manacore/engine": "workspace:*",
    "@manacore/ai": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
